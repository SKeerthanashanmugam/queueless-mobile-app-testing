# Contributing

Use synthetic data only. Link every new test case to a requirement. Do not commit application binaries, credentials, tokens, real patient data or unredacted evidence. Defect examples must be labelled sample unless observed and evidenced on a named authorized build.

